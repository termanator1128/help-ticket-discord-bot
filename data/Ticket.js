var fs = require('fs'),
	path = require('path');

module.exports.Ticket = function(message) {
	return {
		"id": ticketId;
	}
};


/* Messages are automatically written to file
 */
module.exports.Message = function(message) {
	return new Promise(function(resolve, reject) {
		
	});
};